[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["CI=false REACT_APP_API_URL=${REACT_APP_API_URL:-https://teacher-hiring-backend-production.up.railway.app/api} npm run build"]

[start]
cmd = "npx serve -s build -l $PORT"
