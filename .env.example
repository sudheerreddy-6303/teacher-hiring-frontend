# AcadHr Frontend Environment Variables
# Local dev: copy to .env and set your local backend URL.
# Production (Railway) uses .env.production which is committed.
REACT_APP_API_URL=http://localhost:5000/api
