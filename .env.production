# AcadHr — Production Environment Variables (Render)
# Used automatically during `npm run build` for production deploys.
# Overrides .env so local dev (which uses .env) is unaffected.
#
# Frontend (this app) is hosted at: https://teacher-hiring-frontend.onrender.com
# Backend API is hosted at:         https://teacher-hiring-backend.onrender.com
REACT_APP_API_URL=https://teacher-hiring-backend.onrender.com/api
