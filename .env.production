# Production build environment (read automatically by `npm run build`).
# This file IS committed (not gitignored) so Railway bakes in the correct
# backend URL at build time. Local dev still uses .env / the proxy.
REACT_APP_API_URL=https://teacher-hiring-backend-production.up.railway.app/api
